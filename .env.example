# ─── Symfony ──────────────────────────────────────────────────────────────────
# Generate a secure secret with: openssl rand -hex 32
APP_SECRET=changeme_use_a_long_random_string

# ─── Caddy / FrankenPHP ───────────────────────────────────────────────────────
# Use SERVER_NAME=:80 to disable HTTPS (e.g. behind a reverse proxy)
SERVER_NAME=localhost
HTTP_PORT=80
HTTPS_PORT=443
HTTP3_PORT=443
